export function testFun() {
    for (const a of [1, 2, 3]) {
        console.log("webpack for j" + a);
    }
}

export function greetings(text, person) {
    return `${text}, ${person}.----- webpack for js`;
}
